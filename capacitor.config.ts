import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'm_app_sirchai',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
